# Base de données (fournie automatiquement par Railway PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# JWT (change cette valeur — chaîne aléatoire longue)
JWT_SECRET=change_moi_en_production_abc123xyz789
JWT_EXPIRES_IN=7d

# Port (Railway l'injecte automatiquement — ne pas changer)
PORT=3000

# URL du frontend Railway (ex: https://rpg-nexus-frontend.up.railway.app)
FRONTEND_URL=https://TON-FRONTEND.up.railway.app
